#!/usr/bin/node

const printLoopString = () => {
  const loopString = ['C is fun', 'Python is cool', 'JavaScript is amazing'];

  for (const strChar of loopString) { console.log(strChar); }
};

printLoopString();
